# Producción: frontend en Cloud Storage, backend en Cloud Run.
# Sin esta variable, las rutas relativas /avatars/... se piden al dominio del frontend (404).
# Poner la URL pública del backend (Cloud Run), sin barra final.
# Ejemplo:
VITE_API_URL=https://tu-servicio-xxxxxx.run.app
